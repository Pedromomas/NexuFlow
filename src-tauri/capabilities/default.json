{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal permissions for the NexuFlow UI. The webview cannot execute arbitrary shell commands.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
